<%= form_with(model: play_list) do |form| %>
  <% if play_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(play_list.errors.count, "error") %> prohibited this play_list from being saved:</h2>

      <ul>
        <% play_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
